import 'package:flutter/material.dart';

class SupportAndFAQ extends StatelessWidget {
  const SupportAndFAQ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Support and FAQ"),
      ),
    );
  }
}
